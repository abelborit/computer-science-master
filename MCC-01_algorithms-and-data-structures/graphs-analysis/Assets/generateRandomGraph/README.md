En este carpeta se tienen que generar los archivos .txt en cuanto a los matrices ya que GitHub no permite un tamaño superor a 100MB.

Se genera automáticamente corriendo el archivo generateRandomGraph.py
